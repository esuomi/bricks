subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    group = 'io.induct.bricks'
    version = '0.2.0'

    repositories {
        mavenCentral()
    }

    apply from: file("${rootProject.projectDir}/cradle/publishing.gradle")
}
